services:
  - type: web
    name: kitchen-dashboard
    env: python
    plan: free
    startCommand: "python app.py"
    envVars:
      - key: POSTER_TOKEN
        sync: false
      - key: CHOICE_TOKEN
        sync: false
      - key: WEATHER_KEY
        sync: false
